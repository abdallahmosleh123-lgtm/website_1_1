name: website_1_1  # <--- THIS IS THE ONLY REQUIRED CHANGE
description: Emy's Cosmetics Brand Website
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0 # تأكد إن المسافات مظبوطة (مسافتين قبل google_fonts)
  url_launcher: ^6.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true